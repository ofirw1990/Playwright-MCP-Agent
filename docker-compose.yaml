services:
  playwright-agent:
    build: .
    container_name: playwright_sandbox
    volumes:
      - .:/app
    working_dir: /app
    tty: true
    stdin_open: true
    ports:
      - "3100:3100"
    deploy:
      resources:
        limits:
          memory: 2G